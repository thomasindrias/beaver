"""Placeholder — replaced by the real MLX vision server in Task 2.

Exists so Tauri's bundle-resource validation (which checks declared resource
paths at build time) passes after Task 1 declares this file in tauri.conf.json.
"""
